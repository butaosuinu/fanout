// Package arch is a test-only package holding the layer architecture guard:
// the godep-cruiser rule canon (godep-cruiser.json, exceptions in
// godep-cruiser-baseline.json) run via archtest, plus the tree-shape checks
// import analysis cannot cover.
package arch
